using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BattleRoutine : MonoBehaviour
{

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
       
    }
    //check if trainer or wild
        //if trainer, trainer appears and sends out pokemon
        //if not, pokemon appears immediately
    //combatants choose strategy
    //calculate priority
    //resolve strategy
        //if strategy is successful run, battle is over
    //check if pokemon fainted 
        //if yes, check if other pokemon are available (always no if wild)
            //if yes, prompt switch 
            //if not battle is over
        //if not, let trainer choose strategy
}